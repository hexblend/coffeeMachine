import React from 'react';

const Landing = () => {
    return (
        <section className="landing-section">
            <h1>Landing Section</h1>
        </section>
    );
}

export default Landing;